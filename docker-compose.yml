services:
  schulmanager:
    build: .
    container_name: trt-schulmanager
    ports:
      - "8086:8080"
    volumes:
      - schulmanager-data:/data
    environment:
      # Login-Passwort der Web-App — nach Belieben ändern
      - APP_PASSWORD=lehrer2026
    restart: unless-stopped

volumes:
  schulmanager-data:
